# =============================================================================
# Environment Variables for Terraform
# =============================================================================
# Copy this file to .env and fill in your actual values.
# DO NOT commit .env to version control.
#
# Usage:
#   Linux/Mac:
#     source .env
#     terraform apply
#
#   Windows PowerShell:
#     Get-Content .env | ForEach-Object { $var = $_.Split('='); [Environment]::SetEnvironmentVariable($var[0], $var[1]) }
#     terraform apply
#
#   Windows CMD:
#     for /f "tokens=*" %a in (.env) do set %a
#     terraform apply
#
# =============================================================================

# -----------------------------------------------------------------------------
# DigitalOcean Configuration
# -----------------------------------------------------------------------------

# Get your API token from: https://cloud.digitalocean.com/account/api/tokens
# Required: Read + Write permissions
TF_VAR_do_token=dop_v1_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# SSH Configuration
# -----------------------------------------------------------------------------

# SSH Private Key for VPS access
# Option 1: Read from file (Linux/Mac)
# TF_VAR_ssh_private_key=$(cat ~/.ssh/id_rsa | awk 'NF {sub(/\r/, ""); printf "%s\\n",$0;}')

# Option 2: Direct value (use \n for newlines)
TF_VAR_ssh_private_key=-----BEGIN OPENSSH PRIVATE KEY-----\n...\n-----END OPENSSH PRIVATE KEY-----

# -----------------------------------------------------------------------------
# Let's Encrypt Configuration
# -----------------------------------------------------------------------------

# Email for Let's Encrypt certificate registration
# Required when enable_https = true in terraform.tfvars
TF_VAR_app_email=your@email.com

# -----------------------------------------------------------------------------
# Cloudflare Configuration (Optional)
# -----------------------------------------------------------------------------
# Only needed when using Cloudflare DNS or Origin Certificate
# -----------------------------------------------------------------------------

# Cloudflare API Token
# Get from: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Zone - DNS - Edit, Zone - Zone - Read
# TF_VAR_cloudflare_api_token=your-cloudflare-api-token

# Cloudflare Zone ID
# Found in your Cloudflare domain overview
# TF_VAR_cloudflare_zone_id=your-zone-id

# Cloudflare Origin Certificate (PEM format)
# Use \n for newlines in the certificate content
# TF_VAR_cloudflare_origin_cert=-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----

# Cloudflare Origin Certificate Private Key (PEM format)
# TF_VAR_cloudflare_origin_key=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----
